const video = document.getElementById("myVideo");

// Play the video on button click (optional)
// You can replace this with any event listener

// function playVideo() {
//   video.play();
// }

// Pause the video
function pauseVideo() {
  video.pause();
}

// Mute the video
function muteVideo() {
  video.muted = true;
}

// Unmute the video
function unmuteVideo() {
  video.muted = false;
}

// You can add more functionalities like setting volume, seeking position etc.
